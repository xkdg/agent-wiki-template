# YYYY-MM-DD — <сервис>: <короткая суть инцидента>

> Шаблон post-mortem. Скопировать в `wiki/decisions/YYYY-MM-DD-<сервис>-<тема>.md`, удалить этот блок-цитату, заполнить секции.
>
> Decision = дескриптивный лог инцидента: симптом → диагностика → root cause → фикс → урок. **Сырьё, амортизируется.** Когда урок переехал в `PATTERNS` / `ANTI_PATTERNS` / новый ADR — этот файл можно удалить (см. `CLAUDE.md §Принцип амортизации`).
>
> Дата в имени файла — день фиксации post-mortem (не день инцидента, если они расходятся).

**Сервис:** *имя сервиса или `shared/<module>`*
**Триггер:** *как был обнаружен — smoke тест, прод-алерт, ручной запуск с флагом, …*

---

## Симптом

*Что наблюдалось снаружи. Сообщения об ошибках, метрики, поведение пользователя. Без интерпретации.*

---

## Диагностика

*Что делали, чтобы локализовать причину. Минимальный repro, если есть.*

```
# Минимальный repro, если возможен
```

---

## Root cause

*Что именно сломалось и почему. Должна быть видна цепочка причина → следствие, а не просто «сломался X, починили Y».*

---

## Фикс

*Что именно изменили. Файл, функция, в чём суть правки. Не diff целиком — суть.*

```python
# Если уместен короткий до/после
```

---

## Урок (амортизация в долгоживущие артефакты)

*Что переезжает в долгоживущие документы:*
- *`PATTERNS.md` `P-NN` — если родился переиспользуемый приём*
- *`ANTI_PATTERNS.md` `A-NN` — если родилось общее правило «не делайте так»*
- *Новый `ADR` — если решение требует ADR-уровня*
- *Ничего — если случай частный, без обобщения. Тогда decision остаётся как единичный исторический контекст или удаляется.*

*Явно написать, что и куда переехало. Если ничего — обосновать почему.*

---

## Связано

- *Файлы кода — где живёт фикс*
- *`wiki/adr/00NN-...md` — связанный ADR*
- *`wiki/todo/<task>.md` — если инцидент породил задачу*
