# Проверенные паттерны

> Прескриптивная сторона проекта: «как делать правильно». У большинства паттернов
> есть зеркальный антипаттерн в `ANTI_PATTERNS.md` — ссылки указаны в каждом блоке.
> Наполняется из опыта команды и постмортемов в `wiki/decisions/` (по мере появления).
>
> **Правила нумерации** (см. `CLAUDE.md §Правила именования и нумерации`):
> - ID формата `P-NN`, sequential, **никогда не переиспользуется**.
> - В заголовке всегда полный ID: `### P-NN: <название>`.
> - Cross-refs через ID: «См. также: A-22, ADR-0011».

---

## Шаблон записи (удалить после первой реальной записи)

### P-01: <Короткое утверждение приёма>

*1–2 предложения: что делаем и зачем.*

```python
# Если уместен короткий пример кода
```

**Когда применять:** *условие триггера приёма*

**Почему:** *first cause — что ломается без этого приёма*

См. также: *`A-NN`, `ADR-NNNN`*

---
