# <Короткое название задачи в одной строке>

> Шаблон ТЗ. Скопировать в `wiki/todo/<short-kebab-name>.md`, удалить эту блок-цитату и заполнить секции. Курсивом — примеры, как заполнять.

**Сервис / область:** *например: aiohttp-crawl, deep-crawl, shared/crawl, …*
**Создан:** *YYYY-MM-DD*
**Статус:** `draft` | `in-progress` | `done`

---

## Контекст

*2–5 предложений: откуда задача, что без неё плохо, какая боль закрывается. Достаточно, чтобы понять «зачем» без открытия других файлов.*

*Пример:*

*В deep-crawl есть retry-механизм по `deep_crawl_retry_after`, но нет способа явно переоткрыть записи, упавшие с ошибкой, без ожидания backoff. На проде после деплоя фикса нужно вручную пересмотреть ~50 записей с `deep_crawl_done=FALSE` и неправильным `error_code`. Аналог `--retry-errors` уже есть в aiohttp-crawl и закрывает похожий кейс.*

---

## Скоуп

### В рамках задачи

- *Добавить флаг `--retry-errors` в `services/deep-crawl/src/deep_crawl/__main__.py`*
- *По флагу — селектор сбрасывает `deep_crawl_retry_after = NULL` для записей с `deep_crawl_done=FALSE AND error_code IS NOT NULL`*
- *Логирование сколько записей переоткрыто*

### НЕ входит (явно отклонено из скоупа)

*Что искушение тащить, но не тащим. Главная защита от scope creep.*

- *Не меняем логику самого retry-gate — только сбрасываем backoff*
- *Не добавляем фильтр по конкретному `error_code` — переоткрываем все*

---

## Перед стартом — изучить

*Конкретные ID артефактов и файлов кода с пометкой зачем. Не «изучить ADR-0003», а «прочитать ADR-0003 §Hard signal gate, чтобы понять контекст cap=34».*

- *`P-01` — селекторы по статусу/флагу: правильное условие WHERE*
- *`services/aiohttp-crawl/src/aiohttp_crawl/__main__.py` — функция `run_retry_errors`, реюзаем шаблон*
- *`services/deep-crawl/src/deep_crawl/__main__.py:120-150` — где сейчас задаётся retry-селектор*

---

## План реализации

*Список шагов в порядке выполнения. Один шаг = одно действие. Под шагом — нетривиальный нюанс, если есть.*

- [ ] *Шаг 1: Добавить аргумент `--retry-errors` в `argparse`*
- [ ] *Шаг 2: Реализовать функцию `run_retry_errors(conn) -> int`, возвращает количество переоткрытых записей*
  - *SQL: `UPDATE ... WHERE deep_crawl_done=FALSE AND error_code IS NOT NULL`*
- [ ] *Шаг 3: Подвязать в `main()` через ветку `args.retry_errors`*
- [ ] *Шаг 4: Тест в `services/deep-crawl/tests/test_main.py`*

---

## Критерии приёмки

*Проверяемые условия. «Сделано хорошо» — не критерий. «Тест X зелёный», «`SELECT ... WHERE Y` после команды возвращает 0», «`--stats` печатает поле N» — критерии.*

- [ ] *`docker compose run deep-crawl --retry-errors` печатает `Reopened N records` и завершается с кодом 0*
- [ ] *После команды `SELECT count(*) FROM ... WHERE ...` возвращает 0*
- [ ] *Новый юнит-тест зелёный*
- [ ] *Существующие тесты не сломаны*

---

## После реализации обновить

*По матрице документации в `CLAUDE.md`. Отметить `[x]` когда обновил.*

- [ ] *`wiki/todo/BACKLOG.md` — отметить `[x]` пункт «...»*
- [ ] *Этот ТЗ-файл — статус `done` + `[x]` по всем пунктам выше*
- [ ] *Если по пути обнаружится антипаттерн или переиспользуемый приём — `ANTI_PATTERNS.md` / `PATTERNS.md`*
- [ ] *Если задача родила нетривиальную диагностику бага — post-mortem в `wiki/decisions/`*

*Сам ТЗ-файл после `done` можно удалить, если уникальной ценности не осталось (всё в коде + git log + долгоживущих артефактах).*

---

## Открытые вопросы

*Опционально. Удалить секцию, если нет.*

- *Нужно ли логировать каждую запись или достаточно агрегата? — решить при реализации шага 2*

---

## Риски

*Опционально. Удалить секцию, если нет.*

- *Если на проде есть записи с `deep_crawl_retry_after` в будущем (legitimate backoff), переоткроем их тоже. Цена ограничена.*
